name: release

# Build cross-platform binaries for both agentmail-server and agentmail-gateway
# on tag push. Pure Go, no cgo — single static binary per target.
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: windows
            goarch: amd64
            ext: ".exe"
          - goos: linux
            goarch: amd64
            ext: ""
          - goos: linux
            goarch: arm64
            ext: ""
          - goos: darwin
            goarch: amd64
            ext: ""
          - goos: darwin
            goarch: arm64
            ext: ""
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true

      - name: Build both binaries
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          go build -trimpath -ldflags="-s -w -X github.com/agentmail/agentmail/internal/server.Version=${GITHUB_REF_NAME}" -o agentmail-server${{ matrix.ext }} ./cmd/agentmail-server
          go build -trimpath -ldflags="-s -w" -o agentmail-gateway${{ matrix.ext }} ./cmd/agentmail-gateway

      - name: Package
        run: |
          name="agentmail-${GITHUB_REF_NAME}-${{ matrix.goos }}-${{ matrix.goarch }}"
          mkdir -p dist
          cp agentmail-server${{ matrix.ext }} agentmail-gateway${{ matrix.ext }} README.md LICENSE deploy/agentmail.toml.example dist/
          if [ "${{ matrix.goos }}" = "windows" ]; then
            cd dist && zip -r "../${name}.zip" . && cd ..
          else
            tar czf "${name}.tar.gz" -C dist .
          fi

      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            agentmail-*.tar.gz
            agentmail-*.zip
